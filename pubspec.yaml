name: bitik_bi
description: "A marketplace app for Senegal."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # Add this line
    sdk: flutter    # And this line
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  geolocator: ^14.0.1
  image_picker: ^1.1.2
  sqflite: ^2.3.3
  intl: ^0.20.2
  provider: ^6.1.2
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/translations/
    - assets/images/
  generate: true